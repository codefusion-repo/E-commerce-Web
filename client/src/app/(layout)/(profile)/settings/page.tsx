// `app/profile/settings/page.tsx` is the UI for the `/profile/settings` URL

import Settings from "../../../../components/profile/settings/settings";

export default function Page() {
  return <Settings />;
}
