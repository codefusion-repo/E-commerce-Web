// `app/checkout/delivery page.tsx` is the UI for the `/checkout/delivery` URL

import Delivery from "../../../components/checkout/delivery/delivery";

export default function Page() {
  return <Delivery />;
}
