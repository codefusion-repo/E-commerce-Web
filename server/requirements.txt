django==4.2,<5.0                                       # Framework web de Python para el desarrollo rápido y limpio de aplicaciones.
django-environ==0.11.2                              # Simplifica la gestión de configuraciones utilizando variables de entorno en entornos Django.
dj-database-url==2.1.0                              # Una utilidad de Django que te permite utilizar la variable de entorno DATABASE_URL para configurar tu aplicación.
django-cors-headers==3.10.1                          # Habilita el soporte para Cross-Origin Resource Sharing (CORS) en aplicaciones Django, permitiendo solicitudes desde diferentes dominios.
djangorestframework==3.14.0                         # Un potente marco para la construcción de API web en Django, proporcionando herramientas para la creación de servicios RESTful.
djangorestframework-simplejwt==5.3.1               # Extensión para Django REST Framework que implementa JSON Web Token (JWT) para la autenticación de usuarios en API.
django-storages==1.12.3                             # Facilita el manejo del almacenamiento en la web, permitiendo la conexión a varios servicios de almacenamiento en la nube.
django-cleanup==8.0.0                               # Ofrece funciones para limpiar y borrar automáticamente archivos de imágenes antiguos o no utilizados del servidor, optimizando el espacio.
django-tinymce==3.6.1                              # Un widget de Django para campos de texto con el editor WYSIWYG TinyMCE.

# django-channels==0.7.0 HERE
# django-nextjs==3.2.0 HERE

argon2-cffi==21.3.0                                 # Implementa Argon2 utilizando CFFI, proporcionando una forma segura y eficiente de manejar contraseñas en Python.
psycopg2-binary==2.9.11                                 # Adaptador de PostgreSQL para Python, versión construida desde el código fuente, más segura y compatible que la versión binaria.

Pillow==12.2.0                                    # Biblioteca de procesamiento de imágenes en Python que agrega soporte para abrir, manipular y guardar varios formatos de imágenes.
phonenumbers==8.13.28                               # Una biblioteca de Python para analizar, formatear y validar números de teléfono internacionalmente.
humanize==3.14.0                                     # Una biblioteca de Python que te ayuda a formatear números, fechas, tiempos y tamaños de archivos de una manera más legible para los humanos.
#slugify==0.0.1                                      # Una utilidad genérica para crear 'slugs' inspirada en el filtro de plantilla slugify de Django.

firebase-admin==5.4.0                              # El SDK de Firebase Admin para Python, que te permite acceder a los servicios de Firebase desde entornos privilegiados.
requests==2.27.1                                   # Una biblioteca de Python que te permite enviar solicitudes HTTP de una manera simple y elegante.
mercadopago==2.2.2

whitenoise==5.3.0                                   # Una biblioteca de Python que te permite servir archivos estáticos de forma eficiente desde tu aplicación web.
#gunicorn==21.2.0                                    # Un servidor HTTP WSGI para Python, compatible con varios frameworks web y ligero en recursos del servidor.
boto3==1.23.10                                    # El AWS SDK para Python, que te permite interactuar con los servicios de AWS desde tu aplicación Python.
mailjet-rest==1.3.4   
uvicorn==0.17.0
cryptography==40.0.2